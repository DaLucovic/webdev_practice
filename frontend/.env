# Vite exposes variables prefixed with VITE_ to the browser bundle.
# Variables without that prefix are server-side only and are NOT included in the build.
# Python equivalent: os.getenv("VITE_API_BASE_URL")
VITE_API_BASE_URL=/api
